# ============================================================
# 芯宝 (Xinbao) - 香橙派 AIPro / ARM Linux 依赖清单
# 全部使用 CPU 版本，无 CUDA 依赖
# ============================================================

# --- Web 框架 ---
flask==3.1.3
flask-cors==6.0.2
waitress==3.0.2

# --- 向量数据库 & 检索 ---
chromadb==1.5.9
sentence-transformers==5.5.1

# --- 深度学习 (CPU-only, ARM64 兼容) ---
torch>=2.0.0
torchvision>=0.15.0
transformers==5.8.1
# 注意：香橙派上不要装 CUDA 版本，pip install torch 会自动选 CPU 版

# --- 语音合成 ---
edge-tts==7.2.8

# --- 意图分类 ---
scikit-learn==1.7.2
jieba==0.42.1

# --- 工具库 ---
requests==2.34.2
numpy>=1.24.0
python-dotenv==1.2.2

# --- 模型下载 (魔搭高速通道) ---
modelscope==1.37.0
